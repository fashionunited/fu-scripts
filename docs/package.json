{
  "scripts": {
    "build": "fu-scripts build -d core",
    "build:watch": "npm run build -- --watch",
    "format": "fu-scripts format",
    "lint": "fu-scripts lint",
    "lint:md": "markdownlint -i node_modules -i dist .",
    "test": "fu-scripts test",
    "validate": "fu-scripts validate"
  }
}
